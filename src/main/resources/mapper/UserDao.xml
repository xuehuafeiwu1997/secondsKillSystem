<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.xmy.secondskill.dao.UserDao">    <select id="findMobileAndPasswordById" resultType="com.xmy.secondskill.entity.User">               select id from miaosha_user where id = #{id};    </select>    <select id="getById" resultType="com.xmy.secondskill.entity.User">        select * from miaosha_user where id = #{id};    </select>    <!--这里没有进行测试 -->    <update id="updatePassword">        update miaosha_user set password = #{password} where id = #{id};    </update></mapper>